# Amazon FBA Tracker Nginx Configuration (Port 8080)
# Bu dosyayı /etc/nginx/sites-available/amazon-fba-tracker olarak kaydedin

server {
    listen 8080;  # Port 8080'de çalışacak
    server_name 192.168.1.250;
    
    # Root directory - build edilmiş dosyalar
    root /var/www/amazon-fba-tracker/dist;
    index index.html;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;
    
    # Main location block
    location / {
        try_files $uri $uri/ /index.html;
        
        # Cache static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
    
    # Supabase API proxy (CORS için)
    location /supabase/ {
        proxy_pass https://rwxkjsnnemzuxtrzygzq.supabase.co/;
        proxy_http_version 1.1;
        proxy_set_header Host rwxkjsnnemzuxtrzygzq.supabase.co;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Error pages
    error_page 404 /index.html;
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

# Ana ERP uygulaması için (Port 80 - mevcut)
server {
    listen 80;
    server_name 192.168.1.250;
    
    # ERP uygulamanızın mevcut konfigürasyonu buraya gelecek
    # Örnek:
    # location / {
    #     proxy_pass http://localhost:3000;  # ERP uygulamanızın portu
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection 'upgrade';
    #     proxy_set_header Host $host;
    #     proxy_cache_bypass $http_upgrade;
    # }
}
